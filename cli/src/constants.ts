/** 默认内容 ref，跟随 main 即始终最新。 */
export const DEFAULT_REF = 'main';

export const LOCK_FILE_NAME = 'siku-lock.json';

/** AGENTS.md 注入的标记区块边界，保证追加幂等。 */
export const AGENTS_MD_START = '<!-- siku:start -->';
export const AGENTS_MD_END = '<!-- siku:end -->';

export const VERSION = '0.1.0';
